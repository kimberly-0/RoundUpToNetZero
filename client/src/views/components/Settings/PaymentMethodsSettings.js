export default function PaymentMethodsSettings({ userId }) {
    return (
        <div className='page-table-container component-container'>
            <h2 className='section-title'>Payment methods</h2>

        </div>
    )
}
